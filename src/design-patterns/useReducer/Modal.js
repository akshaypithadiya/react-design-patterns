import React from "react";

const Modal = () => {
  const style = {
    border: "1px solid black",
    borderRadius: "4px",
    padding: "6px",
    maxWidth: " 200px",
    margin: "auto",
    marginBottom: "10px",
  };
  return <h3 style={style}>modal</h3>;
};

export default Modal;
